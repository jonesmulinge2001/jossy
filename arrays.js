let courses = ['Python', 'Angular', 'React', 'Nestjs']
// arrays are indexed => actual length-1
console.log(courses.length)
console.log(courses[2])

// sort, search, pop, combine, filter, find, reduce, remove, add